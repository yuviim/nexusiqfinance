# Copy to .env.local for local overrides (Vite auto-loads .env.local, gitignored by default).
# Leave unset for local dev — it falls back to http://localhost:5050 automatically.
# In Vercel, set this in Project Settings → Environment Variables instead of a file.

VITE_API_BASE_URL=http://localhost:5050
