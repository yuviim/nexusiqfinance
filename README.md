# NexusIQ Finance

A personal finance tracker with three Claude-powered agents (advisor, expense
tracker, tax auditor).

- `backend/` — Flask API, SQLite, the three agents. See `backend/README.md`.
- `web/` — React + Vite frontend. See `web/README.md`.
